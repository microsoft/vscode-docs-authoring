This is a stub types definition for jest-diff (https://github.com/facebook/jest/tree/master/packages/jest-diff).

jest-diff provides its own type definitions, so you don't need @types/jest-diff installed!